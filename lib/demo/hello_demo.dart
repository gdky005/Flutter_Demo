import 'package:flutter/material.dart';

import 'listview_demo.dart';

class Hello extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: ListViewDemo());
  }
}
